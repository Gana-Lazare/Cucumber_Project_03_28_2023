pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                // Add build steps here, such as compiling your Java code
               // sh 'javac .java'
            }
        }

        stage('Test') {
            steps {
                // Add test steps here, such as running unit tests
                //sh 'java -cp . MyTestFile'
                sh 'mvn clean test'
            }
        }

        stage('Deploy') {
          steps {
                          echo 'Deploying....'
                      }
                      }
                      }
                      }
